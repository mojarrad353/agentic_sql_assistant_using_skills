# Prometheus scrape configuration for the SQL Assistant
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "sql-assistant-api"
    static_configs:
      - targets: ["api:8000"]
    metrics_path: "/metrics"
